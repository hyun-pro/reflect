{
  "name": "reflect-backend",
  "version": "0.1.0",
  "description": "Reflect — Cloudflare Workers backend (RAG + Claude reply suggestions)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secrets:put": "echo Use: wrangler secret put SUPABASE_URL etc."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@supabase/supabase-js": "^2.50.0",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.20.0"
  }
}
